import React, { useState } from "react";
import Router from "./Routes/router"; // Make sure the path is correct
import "./styles.css";
const App = () => {
  return <Router />;
};

export default App;
